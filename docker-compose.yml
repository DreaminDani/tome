version: '3'
services:
  db:
    image: postgres
    restart: always
    volumes:
      - db-data:/var/lib/postgresql/data
    env_file: .env
  db-migrate:
    image: db-migrate:latest
    build: ./db
    restart: on-failure
    depends_on:
      - db
    env_file: .env
  adminer:
    image: adminer
    restart: always
    ports:
      - 7772:8080

volumes:
  db-data:
    driver: local